[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    slow: long-running tests (deselect with '-m "not slow"')
    integration: tests requiring a live daemon/browser subprocess
    llm: tests requiring an LLM API key
    browser: tests requiring Playwright chromium
